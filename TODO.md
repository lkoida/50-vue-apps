- [ ] Ленивая загрузка и динамический роутинг (Router)
  - [ ] Если у тебя есть навигация по этим 50 приложениям, не импортируй их все сразу в `router/index.js`. 
  - [ ] Что добавить: Сделай ленивую загрузку компонентов (`Lazy Loading) через () => import(...)`. 
  - [ ] Уровень профи: Напиши скрипт (или используй возможности Vite/Webpack), который будет динамически сканировать папку `views/apps` и автоматически генерировать массив маршрутов _(Automated Routing)_.
   - [ ] На экзамене любят вопросы про оптимизацию бандла.
- [ ] Кэширование состояния (`<KeepAlive>`)
   - [ ] По умолчанию, переходя от "Приложения 1" к "Приложению 2", первое уничтожается. 
   - [ ] Что добавить: Оберни твой `<router-view>` во встроенный компонент `<KeepAlive>`. 
   - [ ] Сделай так, чтобы пользователь мог начать вводить текст в проекте "To-Do List", перейти в проект "Калькулятор", вернуться, и его текст остался на месте. Заодно попрактикуешь хуки onActivated и onDeactivated.
- [ ] Асинхронность из коробки (`<Suspense>`)
   - [ ] Наверняка среди 50 проектов есть те, что ходят в API (погода, цитаты, генератор пользователей). 
   - [ ] Что добавить: Вместо того чтобы писать локальные isLoading в каждом компоненте, используй корневой компонент `<Suspense>`.
   - [ ] Сделай setup в компоненте асинхронным (просто `await fetch(...)` прямо на верхнем уровне `<script setup>`), а `<Suspense>` в родителе сам покажет красивый скелетон или лоадер.
- [ ] Фабрика Композаблов (Composables)
  - [ ] В 50 проектах 100% есть дублирующаяся логика. 
  - [ ] Что добавить: Создай папку composables и вынеси туда универсальные функции:
  - [ ] `useFetch.js` — для работы с API с обработкой ошибок. 
  - [ ] `useLocalStorage.js` — чтобы сохранять прогресс в мини-играх или списки задач. 
  - [ ] `useWindowSize.js` — для адаптивной логики (например, прятать сайдбар на мобилках).
- [ ] Независимые сторы Pinia
  - [ ]Что добавить: Создай один глобальный `Setup Store` (например, useThemeStore для переключения светлой/темной темы во всех 50 приложениях сразу). А для самых сложных приложений (например, корзины интернет-магазина) сделай локальные сторы, чтобы отвязать бизнес-логику от UI-компонентов.
